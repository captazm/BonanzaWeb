rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /products/{product} {
      allow read: if true;
      allow write: if true; // Temporary for migration, should be restricted later
    }
    match /posts/{post} {
      allow read: if true;
      allow write: if true;
    }
  }
}
